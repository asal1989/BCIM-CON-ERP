{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf frontend/node_modules && npm install --prefix frontend --include=dev && NODE_OPTIONS=--max-old-space-size=8192 npm run build --prefix frontend && npm install --prefix backend"
  },
  "deploy": {
    "startCommand": "node backend/src/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
